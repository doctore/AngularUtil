/**
 * Interface used to specify the required properties added to the different environment definition.
 */
export interface EnvironmentConfiguration {
  name: string;
  production: boolean;
}
